var num;
/*while(x<=10)
{
    console.log(`${x} * ${i} = ${result}`);
    x++;
}*/

for(i = 1; i <= 10; i++) {

    // multiply i with number
    const result = i * num;

    // display the result
    console.log(`${num} * ${i} = ${result}`);
}